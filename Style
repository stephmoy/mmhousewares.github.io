/* MM Housewares Hardware — shared styles */

:root {
  --brick: #b1502f;
  --brick-dark: #8f3d22;
  --navy: #22303f;
  --cream: #faf5ee;
  --gold: #d99a3f;
  --gray: #5c6670;
  --border: #e4dccf;
  --white: #ffffff;
  --radius: 10px;
  --shadow: 0 2px 8px rgba(34, 48, 63, 0.08);
}

* { box-sizing: border-box; }

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
  background: var(--cream);
  color: var(--navy);
  line-height: 1.6;
}

h1, h2, h3, h4 {
  font-family: Georgia, "Times New Roman", serif;
  margin-top: 0;
  line-height: 1.25;
}

a { color: var(--brick); text-decoration: none; }
a:hover { text-decoration: underline; }

img { max-width: 100%; display: block; }

.container {
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 24px;
}

/* Header */
.site-header {
  background: var(--navy);
  color: var(--white);
  position: sticky;
  top: 0;
  z-index: 10;
}

.site-header .container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: 16px;
  padding-bottom: 16px;
  flex-wrap: wrap;
  gap: 12px;
}

.logo {
  display: flex;
  align-items: baseline;
  gap: 8px;
}

.logo a {
  color: var(--white);
  font-family: Georgia, serif;
  font-size: 1.4rem;
  font-weight: bold;
  text-decoration: none;
  letter-spacing: 0.3px;
}

.logo span {
  color: var(--gold);
  font-size: 0.75rem;
  font-family: -apple-system, sans-serif;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.site-header nav ul {
  list-style: none;
  display: flex;
  gap: 22px;
  margin: 0;
  padding: 0;
  flex-wrap: wrap;
}

.site-header nav a {
  color: var(--white);
  font-size: 0.95rem;
  opacity: 0.88;
}

.site-header nav a:hover,
.site-header nav a.active {
  opacity: 1;
  text-decoration: none;
  border-bottom: 2px solid var(--gold);
}

/* Hero */
.hero {
  background: linear-gradient(135deg, var(--brick) 0%, var(--brick-dark) 100%);
  color: var(--white);
  padding: 64px 0 56px;
}

.hero h1 {
  font-size: 2.4rem;
  margin-bottom: 12px;
}

.hero p {
  font-size: 1.15rem;
  max-width: 600px;
  opacity: 0.95;
}

.hero .tagline {
  color: var(--gold);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  font-size: 0.85rem;
  font-weight: bold;
  margin-bottom: 8px;
}

.hero-buttons {
  margin-top: 24px;
  display: flex;
  gap: 14px;
  flex-wrap: wrap;
}

.btn {
  display: inline-block;
  padding: 12px 24px;
  border-radius: var(--radius);
  font-weight: bold;
  font-size: 0.95rem;
  cursor: pointer;
  border: 2px solid transparent;
}

.btn-primary {
  background: var(--gold);
  color: var(--navy);
}
.btn-primary:hover { background: #c78a30; text-decoration: none; }

.btn-outline {
  background: transparent;
  border-color: var(--white);
  color: var(--white);
}
.btn-outline:hover { background: rgba(255,255,255,0.12); text-decoration: none; }

/* Sections */
.section {
  padding: 56px 0;
}

.section-intro {
  max-width: 680px;
  margin-bottom: 36px;
}

.section-intro .eyebrow {
  color: var(--brick);
  text-transform: uppercase;
  font-size: 0.8rem;
  font-weight: bold;
  letter-spacing: 1px;
  margin-bottom: 6px;
  display: block;
}

/* Card grid */
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 22px;
}

.card {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 24px;
  box-shadow: var(--shadow);
}

.card h3 {
  font-size: 1.15rem;
  margin-bottom: 8px;
}

.card p {
  color: var(--gray);
  font-size: 0.95rem;
  margin-bottom: 14px;
}

.card .icon {
  font-size: 1.8rem;
  margin-bottom: 10px;
  display: block;
}

.card .card-link {
  font-weight: bold;
  font-size: 0.9rem;
}

/* Alt background section */
.section-alt {
  background: var(--white);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}

/* Badges / tags */
.tag {
  display: inline-block;
  background: rgba(177, 80, 47, 0.1);
  color: var(--brick-dark);
  font-size: 0.78rem;
  font-weight: bold;
  padding: 3px 10px;
  border-radius: 100px;
  margin-bottom: 10px;
}

/* Lists for directories */
.directory-list {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.directory-item {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 20px 24px;
  box-shadow: var(--shadow);
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 16px;
  flex-wrap: wrap;
}

.directory-item h3 {
  font-size: 1.05rem;
  margin-bottom: 4px;
}

.directory-item .meta {
  color: var(--gray);
  font-size: 0.88rem;
}

.directory-item .contact {
  text-align: right;
  font-size: 0.9rem;
  min-width: 160px;
}

/* Placeholder note banner */
.placeholder-note {
  background: #fdf3e2;
  border: 1px solid var(--gold);
  color: #6b4c15;
  border-radius: var(--radius);
  padding: 14px 20px;
  font-size: 0.9rem;
  margin-bottom: 32px;
}

/* Footer */
.site-footer {
  background: var(--navy);
  color: var(--white);
  padding: 40px 0 24px;
  margin-top: 40px;
}

.footer-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 24px;
  margin-bottom: 24px;
}

.footer-grid h4 {
  color: var(--gold);
  font-size: 0.95rem;
  margin-bottom: 10px;
}

.site-footer a {
  color: var(--white);
  opacity: 0.85;
}

.site-footer p, .site-footer li {
  font-size: 0.9rem;
  opacity: 0.85;
}

.site-footer ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.site-footer li {
  margin-bottom: 6px;
}

.footer-bottom {
  border-top: 1px solid rgba(255,255,255,0.15);
  padding-top: 18px;
  font-size: 0.82rem;
  opacity: 0.7;
  text-align: center;
}

@media (max-width: 640px) {
  .hero h1 { font-size: 1.8rem; }
  .site-header .container { flex-direction: column; align-items: flex-start; }
}
